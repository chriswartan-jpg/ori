# Printed by `supabase start`. Local dev defaults, not secrets.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Only needed once something has to bypass RLS. Nothing in the MVP does.
SUPABASE_SERVICE_ROLE_KEY=
